[default]
; Shared param
hidden-msg={{ shared_params.hidden_msg }}

; Secret
the-answer={{ secrets.the_answer }}

[logging]
; Normal param
log-level={{ params.log_level }}

; Value from app.yml file
splunk-log-path={{ app_app.splunk[0].log_path }}
