{
    "port": "{{ params.port }}",
    "access_code": "{{ secrets.access_code }}",
    "comment": "Arthur Dent",
    "nodeId": "{{ tsunami_node_id | default('0') }}",
    "primary": "{{ 'true' if tsunami_node_id == 1 else 'false' }}"
}
